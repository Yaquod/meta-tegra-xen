CONFIG_DRM_VIRTIO_GPU=y       # host-side virtio-gpu (for virglrenderer use)
CONFIG_VHOST_VSOCK=y          # vsock host side for SCUDA transport
CONFIG_XEN_VIRTIO=y           # virtio under Xen
CONFIG_IOMMU_SUPPORT=y        # Tegra SMMU for GPU isolation